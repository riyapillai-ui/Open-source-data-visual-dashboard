# Student Performance Dashboard

## Run

The Replit workflow runs Streamlit on port 5000:

```bash
streamlit run app.py --server.address=0.0.0.0 --server.port=5000
```

The dashboard reads `data/student_performance.csv`. Replace the bundled sample
with the final public dataset before submission if required.